<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.questionnaire.michaelbabenkov.questionnaire">

    <application
        android:name=".CoreApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.welcome.WelcomeActivity"
            android:theme="@style/AppTheme.Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/lbl_booster"
            android:theme="@style/AppTheme.Main"/>

        <activity
            android:name=".ui.questionnaire.QuestionnaireActivity"
            android:label="@string/lbl_questionnaire"
            android:theme="@style/AppTheme.Questionnaire"/>

        <activity
            android:name=".ui.submit.SubmitActivity"
            android:label="@string/lbl_submit"
            android:theme="@style/AppTheme.Submit"/>
    </application>

</manifest>